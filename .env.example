# =============================================================================
# Default configuration for rk3588-build.
# Copy this file to .env and adjust the values for your environment.
# =============================================================================

# Board profile. Required for all component and image builds.
# Run 'make list-boards' to see available boards.
# Run 'make use-board' for an interactive picker, or set directly:
BOARD=rk3588s-rock-5c

# Root filesystem. Required for rootfs and image targets: buildroot, debian, or all.
ROOTFS=buildroot

# Debian release: 11 (bullseye), 12 (bookworm), or 13 (trixie).
DEBIAN_RELEASE=13

# Extra Debian APT packages (exact package names, comma or space separated).
# Empty: use board DEBIAN_PACKAGES_DEFAULT if set, else minbase only.
# Force minbase over board default: DEBIAN_PACKAGES=none
# Example: DEBIAN_PACKAGES=network-manager,wpasupplicant,i2c-tools,htop
DEBIAN_PACKAGES=

# Additional APT packages appended to DEBIAN_PACKAGES / board defaults.
# Example: DEBIAN_EXTRA_PACKAGES=htop,python3-pip
DEBIAN_EXTRA_PACKAGES=

# WiFi/BT firmware plugin (optional; not needed for wired-only).
# Controlled by WIFIBT_CHIP, not by package tokens.
# Chip: none|AIC8800D80|AP6275S|AP6256|RTL8822CS|ALL_AP|ALL_CY|ALL
# Source: sdk-or-assets|sdk|assets
# Required: yes fails build if firmware missing; no only warns
WIFIBT_CHIP=
WIFIBT_SOURCE=
WIFIBT_REQUIRED=

# Optional hostname for Debian images (default: board name).
ROOTFS_HOSTNAME=

# Development account created in Buildroot and Debian images.
# Change the password before deploying outside an isolated lab network.
ROOTFS_USERNAME=user
ROOTFS_PASSWORD=password

# Debian mirrors.
DEBIAN_MIRROR=http://deb.debian.org/debian
DEBIAN_SECURITY_MIRROR=http://security.debian.org/debian-security
DEBIAN_ALLOW_ARCHIVE_FALLBACK=yes

# SDK source selection.
# SDK volume: set once here, all make commands inherit it automatically.
# When BOARD is set and SOURCE_MANIFEST is defined in the board profile,
# SDK_VOLUME is auto-derived if left empty. You can also override it here.
# Run 'make use-volume' for an interactive picker.
SDK_VOLUME=rk3588-sdk-rock5c
MANIFEST=
CUSTOM_MANIFEST_URL=
CUSTOM_MANIFEST_NAME=
BRANCH=main

# Fetch and build controls.
FETCH_ON_START=no
JOBS=0
DEPTH=1
MAX_RETRIES=3
EXTRA_COMPONENTS=no
CCACHE_MAXSIZE=10G
USE_NATIVE_BUILD=no

# Final .img.zst compression level.
ZSTD_LEVEL=6
